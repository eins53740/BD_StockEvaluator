"""
Utility helpers used across the Stock Evaluator project.

The test-suite expects a ``yfinance_utils`` module to be available under the
``utils`` package, so we expose it here to make ``utils`` a proper package.
"""
