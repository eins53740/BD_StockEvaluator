name: backend-ci

on:
  push:
    paths:
      - "FlowchartStocks/stock-evaluator/**"
      - ".github/workflows/backend-ci.yml"
  pull_request:
    paths:
      - "FlowchartStocks/stock-evaluator/**"
      - ".github/workflows/backend-ci.yml"

env:
  WORKDIR: FlowchartStocks/stock-evaluator
  PYTHON_VERSION: "3.11"

jobs:
  test-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        working-directory: ${{ env.WORKDIR }}
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      # - name: Ruff lint
      #   working-directory: ${{ env.WORKDIR }}
      #   run: |
      #     ruff check .

      # - name: Black format check
      #   working-directory: ${{ env.WORKDIR }}
      #   run: |
      #     black --check .

      - name: Run tests
        working-directory: ${{ env.WORKDIR }}
        run: |
          pytest tests
